version: '2'
services:
    mims-postgresql:
        image: postgres:9.6.5
        # volumes:
        #     - ~/volumes/jhipster/mims/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=mims
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
